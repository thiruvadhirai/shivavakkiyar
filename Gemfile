source "https://rubygems.org"

# Jekyll and dependencies (required for site generation)
gem "jekyll", "~> 4.4.1"
gem "webrick"

# NOTE: ruby-lsp is NOT included here because:
# 1. It's only needed for IDE autocomplete in local development
# 2. It has native extension dependencies that fail to compile in Alpine containers
# 3. It's not required for Jekyll site generation or running tests
# For local IDE support, install ruby-lsp separately in your IDE/editor setup
